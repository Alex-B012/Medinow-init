const MyProfileInformationTitle = ({ title }) => {
  return (
    <h4 className="my-profile-information-title mb-5  text-emerald-900 font-semibold uppercase underline">
      {title}
    </h4>
  );
};

export default MyProfileInformationTitle;
